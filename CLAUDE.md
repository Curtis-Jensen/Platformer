# Platformer — Claude Instructions

## Git

- **Never run `git commit` or `git push` automatically.**
  Always show the diff / staged files and wait for explicit approval before committing or pushing anything.
